# SwiftShader (swangle) JITs shaders, needs W+X memory; drop once real GPU/Mesa lands.
allow system_server self:process execmem;
gpu_access(system_server)
get_prop(system_server, vendor_graphics_prop)
